# 项目地图

> AI 首次进入仓库时先读本文件，再按任务跳转到 `功能列表/`；只有定位到具体入口后才读源码。

## 30 秒结论

- 产品：RikkaHub 的非官方 Android LLM 客户端 fork，核心增量是自动上下文压缩，并持续加入用量管理、语音、朋友圈等本地能力。
- 主体：`app` 是业务编排和 Android UI；`ai` 是模型、供应商和消息协议底座；其余 Gradle 模块是可复用能力模块。
- 状态：偏好配置存 DataStore，业务实体存 Room，本地附件由 `FilesManager` 管理，网络模型请求经 `ProviderManager` 分发。
- 入口：应用初始化看 `RikkaHubApp.kt`，页面导航看 `RouteActivity.kt`，聊天主流程看 `ChatService.kt`，依赖装配看 `app/.../di/`。

## L0：产品系统

`RikkaHub Android App`

- 面向多供应商 LLM 的本地聊天客户端。
- 支持助手、记忆、MCP、本地工具、搜索、多模态、TTS/ASR、MiniMax Speech 2.6 全局情绪与 ElevenLabs v3/MiniMax Speech 2.8 通话音频标签、Web 访问和本地备份。
- fork 特有维护线：滚动摘要、独立压缩/OCR API、用量提醒与应用锁、朋友圈、语音通话交互。
- 私有本地能力：`personal` flavor 的定时「私有心跳」不随公开构建发布，源码在 git 忽略的 `app/src/personal/`，禁止提交（见 `功能列表/私有心跳.md`）。

## L1：运行边界

| 边界 | 职责 | 主入口 |
|---|---|---|
| Android 应用 | Compose UI、导航、系统服务、权限、通知 | `app/` |
| AI 运行时 | 模型定义、供应商适配、流式协议、工具协议 | `ai/` |
| 本地数据 | Room、DataStore、私有文件、导入导出 | `app/.../data/` |
| Web 端 | Android 内嵌 Ktor 服务与 React 客户端 | `web/`、`web-ui/` |
| 能力模块 | 搜索、语音、文档、渲染、统计、天气 | 同名 Gradle 模块 |
| 发布运维 | universal/arm64 APK 脚本、GitHub Release 更新 | `scripts/`、`UpdateChecker.kt` |
| 私有本地构建 | personal flavor 专属：心跳后台调度、通知与设置页 | `app/src/personal/`（git 忽略，禁止提交） |

## L2：代码分层

| 层 | 目录 | 规则 |
|---|---|---|
| 展示层 | `app/.../ui/` | Compose 页面与组件；页面状态进入对应 VM，不直接拼模型请求。 |
| 编排层 | `app/.../service/` | 跨仓库/模型/工具的长流程；聊天核心由 `ChatService` 统一编排。 |
| 领域与持久化 | `app/.../data/repository/`、`data/db/` | Repository 暴露领域操作；DAO/Entity 只处理存储。 |
| 配置层 | `app/.../data/datastore/` | 全局设置、助手配置、供应商、语音、Web、用量规则。 |
| AI 适配层 | `ai/.../provider/`、`app/.../data/ai/` | `ai` 定义通用协议；`app` 组装提示词、转换器、本地工具和 MCP。 |
| 平台能力 | `service/`、各能力模块 | Android 前台服务、文件、语音、搜索、天气和 Web 服务。 |

## L3：关键修改锚点

| 要改什么 | 先看哪里 |
|---|---|
| 应用启动、前台服务 | `RikkaHubApp.kt`、`service/` |
| 页面和路由 | `RouteActivity.kt`、`ui/pages/<领域>/` |
| 聊天发送、重生成、工具调用、压缩、语音通话状态与挂断事件 | `service/ChatService.kt`、`service/ConversationSession.kt`、`service/VoiceCallRuntimePrompt.kt` |
| 会话树和消息持久化 | `ConversationRepository.kt`、`ConversationDAO.kt`、`MessageNodeDAO.kt` |
| 模型/供应商协议 | `ai/provider/`、`ai/core/` |
| 助手配置、系统提示词和通话背景 | `data/model/Assistant.kt`、`ui/pages/assistant/`、`ui/pages/chat/Background.kt` |
| TTS 供应商、普通聊天文本变换、通话音频标签模式、语音文本清洗与缓存回放、通话翻译清洗 | `speech/.../tts/provider/`、`speech/.../tts/controller/TtsController.kt`、`speech/.../tts/controller/AudioPlayer.kt`、`app/.../ui/hooks/TTS.kt`、`app/.../ui/hooks/ChatTts.kt`、`app/.../utils/TtsTextTransform.kt`、`app/.../data/ai/prompts/VoiceCallAudioTagPrompt.kt`、`app/.../data/voice/VoiceCallAudioTagMode.kt`、`VoiceCallRealtimeEmotion.kt`、`VoiceCallAudioTagCatalog.kt`、`VoiceCallAudioTagSelectionTool.kt`、`VoiceCallAudioTagging.kt`、`VoiceCallTranslationText.kt`、`app/.../service/VoiceCallTextSanitizer.kt`、`app/.../service/ChatService.kt`、`app/.../service/VoiceCallRuntimePrompt.kt`、`app/.../ui/components/message/ChatMessageTranslation.kt` |
| 通话 TTS 队列、播放进度与消息展示投影 | `ui/pages/chat/VoiceCallSpeechPlayback.kt`、`ui/pages/chat/VoiceCallMessagePresentation.kt`；`VoiceCallOverlay.kt` 只负责界面、输入与挂断编排 |
| 全局设置 | `data/datastore/PreferencesStore.kt`、`ui/pages/setting/` |
| 本地工具/MCP/搜索 | `data/ai/tools/`、`data/ai/mcp/`、`search/` |
| 朋友圈 | `MomentsOverlay.kt`、`MomentsVM.kt`、`MomentRepository.kt`、`MomentDAO.kt` |
| 匿名提问箱 | `AnonymousQuestionBoxOverlay.kt`、`AnonymousQuestionBoxVM.kt`、`AnonymousQuestionRepository.kt`、`AnonymousQuestionDAO.kt` |
| 备份恢复 | `ui/pages/backup/`、`data/sync/`、`data/export/` |
| 私有心跳（状态驱动唤醒、主动消息、后台调度、工具白名单、通话期间互斥） | `app/src/personal/.../personal/heartbeat/`、`app/src/main/.../service/VoiceCallSessionRegistry.kt`、`app/src/personal/.../local/LocalBuildIntegration.kt`、`app/src/public/.../local/LocalBuildIntegration.kt` |
| APK 更新 | `utils/UpdateChecker.kt`、`ui/components/ui/UpdateCard.kt` |
| Personal universal debug 打包 | `scripts/personal/build-personal-universal-debug-apk.ps1`、`app/build/outputs/apk/personal/debug/` |

## Gradle 模块职责

| 模块 | 核心职责 | 依赖方向 |
|---|---|---|
| `app` | 产品业务、Compose UI、Room/DataStore、系统服务 | 依赖全部能力模块 |
| `ai` | AI 消息、Tool、模型和 OpenAI/Claude/Google 适配 | 仅依赖 `common` |
| `common` | Android/HTTP/缓存公共工具 | 基础模块 |
| `search` | 多搜索服务统一接口 | 依赖 `ai` |
| `speech` | ASR/TTS 配置、供应商、播放控制 | 依赖 `common` |
| `document` | PDF/DOCX/PPTX/EPUB 文档解析 | 独立能力 |
| `highlight` | 代码高亮 | 独立能力 |
| `material3` | 项目内 Material 组件扩展 | 独立 UI 能力 |
| `usage-tracker` | Android 用量读取、统计模型和页面 | 独立平台能力 |
| `weather` | 定位天气仓库 | 独立平台能力 |
| `web` | Ktor 服务端及静态资源承载 | 构建时依赖 `web-ui` 产物 |

## 目录速览

```text
app/                 Android 主应用和所有业务编排
ai/                  通用 AI 协议与供应商
- 语音标签二次结果由 tagId + replacementText 组成：模型只识别，客户端负责校验前缀、生成供应商格式并构造 TTS 语音副本；启用 TTS 只读英文时，二次模型只接收实际会朗读的纯英文分段，中文及中英混合分段不加标签。
- 通话状态由 `ChatService` 按请求模式注入 system 消息末尾：`VoiceCall` 为 `ACTIVE` 并对完整回复施加 200 字软约束；接通后的首条用户语音还会在仅 API 可见的用户消息副本中携带 `ACTIVE` 事件；挂断后的下一条普通用户消息同样携带 `ENDED` 事件，随后恢复 `INACTIVE`。
common/              公共 Android/HTTP/缓存代码
search/ speech/      搜索与语音能力
document/ highlight/ 文档解析与代码高亮
usage-tracker/       用量统计模型和 UI
weather/             天气能力
web/ web-ui/         Ktor 服务端与 React Web 客户端
scripts/             APK 打包脚本
app/src/public/      public flavor 的本地集成空实现（已跟踪）
app/src/personal/    personal flavor 私有功能：心跳等本地扩展（git 忽略，禁止提交）
scripts/personal/    私有包构建与边界校验（git 忽略，禁止提交）
docs/                现有图片资源
功能列表/            按领域定位功能
任务进展/            已完成、进行中、待处理
```

## 通话状态边界

- `VoiceCallRuntimePrompt.kt` 是 `ACTIVE / ENDED / INACTIVE` 的唯一提示词来源：无论谁发起，接通后的首条用户语音请求副本携带一次 `ACTIVE` 事件；挂断事件由独立记录节点持久化，下一条普通用户消息请求副本携带一次 `ENDED` 事件；两者都不修改原始消息，后续请求不重复携带事件。
- 原始通话消息保留给通话记录 UI 和音频回放，但由 `VoiceCallConversationPresentation.kt` 从普通聊天列表中隐藏，只显示独立记录卡片。

## 私有心跳（personal flavor，仅本机）

- 定时让当前助手后台生成主动消息：默认按念头压力驱动唤醒；普通聊天可通过 `heartbeat_schedule` 让 AI 指定时间、固定间隔或时间窗内随机多次唤醒，后台心跳只执行已保存计划。模型可回复 `[PASS]` 跳过；生成的消息写入最近会话并推送通知，点击回到会话。
- 源码全部位于 git 忽略的 `app/src/personal/`（含 Manifest、strings、`LocalBuildIntegration`），构建走 `personal` flavor（`assemblePersonalDebug`）；公开 `public` flavor 只含 `app/src/public/` 的空实现，不打包任何心跳代码。
- 配置与运行状态存 SharedPreferences `personal_heartbeat`（非 DataStore/Room）；调度用 AlarmManager 精确闹钟 + 开机/覆盖安装重同步；前台服务类型 `specialUse`。
- 工具按白名单过滤后才构造请求：只读/自主直接执行，低风险写入需开关，其余不注入；`ask_user` 改为通知提问。
- 详细能力与入口见 `功能列表/私有心跳.md`。用户明确禁止提交这部分内容（`.gitignore` 已忽略）。
- 晚安模式：用户道晚安后心跳改为每 10 分钟触发一次，用专用提示词检查新增使用并锁定对应软件到第二天白天；连续 3 次无新增使用自动退出。
- 调度双保险：启用后由 `HeartbeatForegroundService` 常驻前台并在服务内部自调度，AlarmManager 负责进程被系统杀死后的冷启动兜底；下次触发以最后一条用户消息为锚点。
- 用户消息持久化后由 `HeartbeatUserActivity` 即时更新消息时间与晚安状态；过期或 `QUEUED` 状态会重新计算/恢复，不以应用打开时间作为用户活动时间。

## 记忆作用域与心跳隔离

- 记忆作用域入口：`app/.../data/model/MemoryScope.kt`；支持助手、全局和 `conversation` 三种作用域。
- 普通聊天、朋友圈和匿名提问箱必须把当前 `conversationId` 传入生成链路；本地记忆工具始终使用助手作用域，不受会话/全局记忆开关影响；心跳固定读取当前会话作用域。
- 心跳后台生成会关闭最近聊天窗口标题参考，只保留当前最近会话的消息历史，避免跨窗口信息泄漏。

## AI 推荐阅读顺序

1. `项目地图.md`
2. 与需求对应的 `功能列表/<领域>.md`
3. `项目规则.md`
4. 领域页列出的 2～5 个入口文件
5. 涉及发布或接续工作时再读 `任务进展/`
- MiniMax Speech 2.8 的标签选择不依赖句首语气词：有语气词就替换，没有语气词也尽量逐段选择标签，只有不适合声音事件时才使用 NONE。
- 通话记录与已保存 TTS 回放：`VoiceCallOverlay.kt` 负责通话界面、输入和挂断，`VoiceCallSpeechPlayback.kt` 负责 TTS 队列、播放进度和音频保存，`VoiceCallMessagePresentation.kt` 负责文本/音频气泡投影；记录卡片作为唯一独立消息节点保存关联消息 ID 与分消息音频映射，原始对话消息不再挂载记录注解，长按卡片会先弹出“是否删除”确认框，确认后删除整段通话消息和音频文件；记录模型与文件入口为 `ai/.../ui/Message.kt`、`data/voice/VoiceCallRecord.kt`、`data/files/FilesManager.kt`。
