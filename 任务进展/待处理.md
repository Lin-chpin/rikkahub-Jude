# 待处理

## 发布链路

- README、应用内更新 API 与发布文档已统一指向当前仓库 `Lin-chpin/rikkahub-Jude`。
- `v1.1.1` 代码线已完成；正式发布时仍需上传名为 `app-universal-debug.apk` 的 universal APK 资产。

## 朋友圈可靠性

- 到期处理依赖打开或刷新朋友圈；若需要后台准时回复，应另行设计 WorkManager/前台任务，不能直接复用 UI 生命周期。

## 文档维护

- 功能或模块边界变化后，同步更新对应领域页，不在 `项目地图.md` 堆叠实现细节。
- 每次版本发布后更新 `已完成.md` 的版本和提交事实。
